export enum Level {
    DEBUTANT = 1, 
    INTERMEDIAIRE = 2, 
    PRO = 3, 
}

export enum TypeMatch {
    PRIVATE = 1,
    PUBLIC = 2,
}

export enum Ambiance {
    FUN = 1,
    COMPETITION = 2,
}

